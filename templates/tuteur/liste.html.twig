{% extends 'base.html.twig' %}

{% block title %}TUTEUR{% endblock %}

{% block body %}

<div>
    <h1>Liste des tuteur</h1>

    <ul>    
        {% for tuteur in tuteurListe %}
            {% if tuteur.roles[0] == 'tuteur' %}

            <li>
            <a href="{{path("tuteur", {id : tuteur.id}) }}"> {{ tuteur.nom}} {{ tuteur.prenom }}</a>
            {# : {{ tuteur.roles[0]}} #}
            </li> 

                <ul>
                    {% for appr in tuteur.apprentis %}
                        <li>{{ appr.nom }} </li>
                    {% endfor %}
                </ul>
            {% endif %}
        {% endfor %}    
    </ul>


</div>
{% endblock %}
